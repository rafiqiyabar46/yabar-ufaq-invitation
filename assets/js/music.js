window.WeddingMusic=(()=>{const button=document.getElementById('musicToggle');const audio=new Audio('assets/music/wedding-music.mp3');audio.loop=true;audio.volume=.18;function setState(isPlaying){button?.setAttribute('aria-pressed',String(isPlaying));button?.setAttribute('aria-label',isPlaying?'Pause background music':'Play background music');if(button)button.querySelector('.music-toggle__text').textContent=isPlaying?'Pause':'Music'}function stop(){audio.pause();setState(false)}async function play(){try{await audio.play();setState(true)}catch(error){setState(false)}}return{bind(){button?.addEventListener('click',()=>audio.paused?play():stop());audio.addEventListener('ended',()=>setState(false));setState(false)}}})();
